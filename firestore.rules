rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Testing rule for the school project: users must be logged in.
    // Replace with stricter production rules later.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
